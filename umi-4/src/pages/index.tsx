// import { useModel } from 'umi'

export default function HomePage() {
  // const { authData } = useModel('auth')

  // console.log(authData);

  return (
    <div>
      <h1>home page</h1>
    </div>
  );
}
